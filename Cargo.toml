[workspace]
members = [
    "program/tabs",
    "clients/rust",
]
resolver = "2"

[workspace.dependencies]
tabs_client = { path = "clients/rust" }

[profile.release]
overflow-checks = true
opt-level       = 3
lto             = true
codegen-units   = 1
